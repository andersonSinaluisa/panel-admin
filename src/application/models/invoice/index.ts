import { createModel } from "@rematch/core";
import { RootModel } from "..";





export const INVOICE = createModel<RootModel>()({
    state: {},
    effects: (dispatch) => ({}),
    reducers: {},
});